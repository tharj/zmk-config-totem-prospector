#pragma once

// The behavior source has loaded ZMK/Zephyr headers before reaching this file.
// Key aliases must follow those headers to avoid names such as CMSIS's Q field.
#include <dt-bindings/zmk/keys.h>
#include_next <zmk-unicode/unicode.h>
